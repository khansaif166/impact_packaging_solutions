{
  "scripts": {
    "preview": "python3 -m http.server 3000",
    "deploy": "wrangler pages deploy . --project-name impact-packaging-solutions"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
